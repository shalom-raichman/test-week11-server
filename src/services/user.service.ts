import UserDTO from '../DTO/user.DTO'

export const register = (user: UserDTO) => {
  try {
    
  } catch (err) {
    
  }
}

export const login = (user: UserDTO) => {
  try {
    
  } catch (err) {
    
  }
}